# HPE Server BIOS Tool (BT) - Python Dependencies
#
# Install with: pip install -r requirements.txt
#
# Note: The Windows installer automatically installs these dependencies.

# Keep system awake during long-running builds
wakepy

# GUI application (optional - only needed for btgui)
PyQt6

# Development/Testing dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
